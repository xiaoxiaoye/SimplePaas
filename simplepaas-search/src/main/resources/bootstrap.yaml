spring:
  application:
    name: search-service
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:123456@localhost:8001/eureka/
server:
  port: 8081